# Current takeaways - Do `member` or `casual` appear to use bikes differently on days of the week?
> Overall, the answer appears to be "Yes, they differ, but not substantially. Both groups exhibit commute-like weekday patterns; members simply show a somewhat stronger morning concentration."


1. `member` accounts for the majority of rides each day
	1. the gap is smallest Sat/Sun
	2. largest gap is Tues/Wed
2.  We see commute-like patterns
	1. An uptick in `total_hourly_rides` starting between 4-5, peaking between 8 - 9, of which `member` is the larger cohort
	2.  `casual` has its relative-peak early AM, and a smaller uptick from 10am - 2pm, when `member` begins its uptick.


